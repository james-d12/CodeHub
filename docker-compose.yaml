services:
  backend:
    build:
      context: ./src/backend
      dockerfile: Backend.Dockerfile
  frontend:
    build:
      context: ./src/frontend
      additional_contexts:
          backend: ./src/backend
      dockerfile: Frontend.Dockerfile